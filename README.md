# Personal prettier config

### Usage

**Install**:
```bash
$ npm install --dev @jgil_r/jg-prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@jgil_r/jg-prettier-config"
}
```
