# Personal prettier config

### Usage

**Install**:
```bash
$ npm install --dev @jgil-r/jg-prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@jgil-r/jg-prettier-config"
}
```
